sudo: required

services:
  - docker

language: ruby

before_install:
  - gem install bundler
  - docker pull  ruby:2.2.4-onbuild

bundler_args: --without debugger

script:
  - bundle exec rake test
  - bundle exec rspec

notifications:
  email:
  - ngiger@ywesee.com

rvm:
  - ruby-head
  - 2.2.4
  - 2.1.7
matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: 1.8.7
